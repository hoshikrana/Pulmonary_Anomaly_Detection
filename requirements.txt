# Pulmonary Anomaly Detection in Chest Radiographs
# requirements.txt
# Install with: pip install -r requirements.txt

torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
Pillow>=9.5.0
matplotlib>=3.7.0
seaborn>=0.12.0
scikit-learn>=1.2.0
scikit-image>=0.20.0   # for SSIM metric
opencv-python>=4.7.0   # for heatmap overlay (Step 6)
flask>=2.3.0           # for web app (Step 7)
tqdm>=4.65.0           # progress bars during training
kaggle>=1.5.13         # dataset download
accelerate>=0.25.0     # EGX runtime dependency (HuggingFace Accelerate)

# EGX Framework — Elastic Guardian X (install separately)
# pip install -e E:\Project\EGX